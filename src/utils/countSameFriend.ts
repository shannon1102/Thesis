import { User } from "../entities/user";

export const  countSameFriend= (x :User, y: User) => {
    // let xx = x.map(e => e.friend?.toString());
    // let yy = y.map(e => e.friend?.toString());
    // let z = xx.filter(function (val) {
    //   return yy.indexOf(val) != -1;
    // });
    // return z.length;
  }
  